<android.support.percent.PercentRelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/subscribe_view"
    android:background="#ff0000"
    tools:context="us.johnchambers.podcast.fragments.SubscribeFragment">

    <ImageView
        android:id="@+id/subscribe_ResultImage"
        app:layout_heightPercent="30%"
        app:layout_aspectRatio="100%"
        android:layout_centerHorizontal="true"
        android:scaleType="fitXY"
        android:padding="16dp"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        android:background="#FF00FF"
        ></ImageView>

    <ListView
        android:id="@+id/subscribeEpisodeListView"
        android:layout_width="match_parent"
        android:background="#ffff00"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        app:layout_heightPercent="70%"
        android:layout_below="@id/subscribe_ResultImage"
        ></ListView>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_subscribe"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_margin="@dimen/fab_margin"
        app:srcCompat="@drawable/ic_add" />

</android.support.percent.PercentRelativeLayout>
